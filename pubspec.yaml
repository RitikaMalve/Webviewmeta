name: guru_samruddhi


description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_launcher_icons: ^0.14.3
  url_launcher: ^6.3.1
  internet_connection_checker: ^3.0.1
  flutter_inappwebview: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "asset/GuruSamruddhi_logo.png"
  remove_alpha_channel_ios: true

flutter:
  uses-material-design: true

  assets:
    - asset/GuruSamruddhi_logo.png
